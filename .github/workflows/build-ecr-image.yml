name: Build ECR Image

on:
  workflow_call:
    inputs:
      aws-region:
        required: true
        type: string
      ecr-repo-name:
        required: true
        type: string
      github-role-arn:
        required: true
        type: string

permissions:
  actions: write
  contents: read
  id-token: write

jobs:
  build-image:
    name: Build Image
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-region: ${{ inputs.aws-region }}
          role-to-assume: ${{ inputs.github-role-arn }}

      - name: Login to ECR
        id: ecr-login
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build image
        env:
          ECR_REGISTRY: ${{ steps.ecr-login.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build --build-arg REVISION=${{ github.sha }} -t $ECR_REGISTRY/${{ inputs.ecr-repo-name }}:$IMAGE_TAG .

      - name: Push image to ECR
        env:
          ECR_REGISTRY: ${{ steps.ecr-login.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker push $ECR_REGISTRY/${{ inputs.ecr-repo-name }}:$IMAGE_TAG

          echo "image-url=$ECR_REGISTRY/${{ inputs.ecr-repo-name }}:$IMAGE_TAG" >> $GITHUB_OUTPUT
